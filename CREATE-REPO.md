# Create GitHub repo + Pages (GRT)

1. Public repo e.g. `grt-quant-options-junior-portfolio`
2. Upload contents of `portfolio/` to repo root
3. Pages → main / root
4. Paste URL into `COVER-NA.txt` and `COVER-APAC.txt`
5. Submit **two** applications if doing both regions
6. Record video; embed Unlisted link in `index.html` when ready

No secrets or live account statements.
